import express, { Request, Response } from "express";
const farmRouter = express.Router();

farmRouter.post("/", async (req: Request, res: Response): Promise<any> => {

})

export default farmRouter;
